import 'package:flutter/material.dart';

class NotIntegratedVendorSheet extends StatelessWidget {
  const NotIntegratedVendorSheet({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
